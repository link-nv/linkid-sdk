--- server/default/deploy/jboss-web.deployer/server.xml~	2008-12-09 11:04:21.000000000 +0100
+++ server/default/deploy/jboss-web.deployer/server.xml	2008-12-09 11:11:15.000000000 +0100
@@ -19,25 +19,28 @@
          APR (HTTP/AJP) Connector: /docs/apr.html
          Define a non-SSL HTTP/1.1 Connector on port 8080
     -->
-    <Connector port="8080" address="${jboss.bind.address}"    
+    <Connector port="${safe-online.jboss.host.port:insecure}" address="${jboss.bind.address}"    
          maxThreads="250" maxHttpHeaderSize="8192"
-         emptySessionPath="true" protocol="HTTP/1.1"
-         enableLookups="false" redirectPort="8443" acceptCount="100"
+         emptySessionPath="false" protocol="HTTP/1.1"
+         enableLookups="false" redirectPort="${safe-online.host.port:secure}" acceptCount="100"
+         proxyName="${safe-online.host.name}" proxyPort="${safe-online.host.port:insecure}"
          connectionTimeout="20000" disableUploadTimeout="true" />
 
     <!-- Define a SSL HTTP/1.1 Connector on port 8443
          This connector uses the JSSE configuration, when using APR, the 
          connector should be using the OpenSSL style configuration
          described in the APR documentation -->
-    <!--
-    <Connector port="8443" protocol="HTTP/1.1" SSLEnabled="true"
+    
+    <Connector port="${safe-online.jboss.host.port:secure}" protocol="HTTP/1.1" SSLEnabled="true"
                maxThreads="150" scheme="https" secure="true"
-               clientAuth="false" sslProtocol="TLS" />
-    -->
+               clientAuth="false" sslProtocol="TLS"
+               proxyName="${safe-online.host.name}" proxyPort="${safe-online.host.port:secure}"
+               keystoreFile="${jboss.server.home.dir}/conf/ssl.jks"
+               keystorePass="secret" /> 
 
     <!-- Define an AJP 1.3 Connector on port 8009 -->
     <Connector port="8009" address="${jboss.bind.address}" protocol="AJP/1.3"
-         emptySessionPath="true" enableLookups="false" redirectPort="8443" />
+         emptySessionPath="false" enableLookups="false" redirectPort="${safe-online.host.port:secure}" />
 
       <Engine name="jboss.web" defaultHost="localhost">
 
