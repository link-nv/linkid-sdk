# Base url to redirect to after successful authentication
# Landing page to finalize a login process.
# Landing page to finalize a logout process.
# Point of return for remote device issuers when authenticating
# Point of return for remote device issuers when registrating
# Point of return for the remote device operations.
# URL used to finalize a logout request sent by the authentication webapp following a single logout process by another application.
# URL used to initiate a login process.
# URL used to initiate a single logout process.
# Url to redirect to after successful authentication
# Used to wrap the servlet request's request URL for verification of the receiving endpoint with the destination in the saml request.
# Used to wrap the servlet request's request URL for verification of the receiving endpoint with the destination in the saml response.


#  Demos
# -------
DemoHostName=${safe-online.host.name}
DemoHostPort=${safe-online.host.port:insecure}

#bank.landingPage=${safe-online.host.auth.basename}/${safe-online.demo.bank.webapp.url}/login 
#bank.logoutExitUrl=${safe-online.host.auth.basename}/${safe-online.demo.bank.webapp.url}/logout 
#bank.logoutLandingPage=${safe-online.host.auth.basename}/${safe-online.demo.bank.webapp.url}/logout 
#bank.logoutUrl=${safe-online.host.auth.basename}/${safe-online.demo.bank.webapp.url}/banklogout 

#cinema.landingPage=${safe-online.host.auth.basename}/${safe-online.demo.cinema.webapp.url}/login 
#cinema.logoutExitUrl=${safe-online.host.auth.basename}/${safe-online.demo.cinema.webapp.url}/logout 
#cinema.logoutLandingPage=${safe-online.host.auth.basename}/${safe-online.demo.cinema.webapp.url}/logout 
#cinema.logoutUrl=${safe-online.host.auth.basename}/${safe-online.demo.cinema.webapp.url}/cinemalogout 

#payment.landingPage=${safe-online.host.auth.basename}/${safe-online.demo.payment.webapp.url}/login 
#payment.logoutExitUrl=${safe-online.host.auth.basename}/${safe-online.demo.payment.webapp.url}/logout 
#payment.logoutLandingPage=${safe-online.host.auth.basename}/${safe-online.demo.payment.webapp.url}/logout 
#payment.logoutUrl=${safe-online.host.auth.basename}/${safe-online.demo.payment.webapp.url}/paymentlogout 
